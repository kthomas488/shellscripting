#!/bin/bash

if [ "$#" -ne 1 ]; then
        echo "Usage $0 input_sv_file"
        exit 1
fi

filename=$1

if [ -f "$filename" ]; then
        echo "File exists"
else
        echo "File does not exist"
fi
